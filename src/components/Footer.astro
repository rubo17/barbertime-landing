---
const currentYear = new Date().getFullYear();

const links = [
	{ label: "Características", href: "#caracteristicas" },
	{ label: "Empieza", href: "#empieza" },
];
---

<footer class="border-t border-border py-12">
	<div class="mx-auto max-w-6xl px-6">
		<div class="flex flex-col items-center justify-between gap-6 md:flex-row">
			<a href="/" class="text-xl font-bold">
				Barber<span class="text-sub">Time</span>
			</a>

			<nav class="flex gap-8">
				{links.map((link) => (
					<a
						href={link.href}
						class="text-sm text-sub transition-colors hover:text-text"
					>
						{link.label}
					</a>
				))}
			</nav>
		</div>

		<div class="mt-8 border-t border-border pt-8 text-center">
			<p class="text-sm text-sub">
				&copy; {currentYear} BarberTime. Todos los derechos reservados.
			</p>
		</div>
	</div>
</footer>
